#include <stdio.h>
int main()
{
  int a, b;

  printf("Give the valu of first intager 1: ");
  scanf("%d", &a);

  printf("Give the valu of Secend intager 2: ");
  scanf("%d", &b);

  a == b;
  b == a;

  printf("the valu of first intager 1:%d \n", a);
  printf("the valu of Secend intager 2:%d \n", b);

  return 0;
}