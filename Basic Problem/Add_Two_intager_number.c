#include <stdio.h>
int main()
{
    int a, b, add;

    printf("Give the valu of first intager Number: ");
    scanf("%d", &a);

    printf("Give the valu of Secend intager Number: ");
    scanf("%d", &b);

    add = a + b;
    printf("The total number of two intager valu:%d\n", add);

    
    return 0;
}